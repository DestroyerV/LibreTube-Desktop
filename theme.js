import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    background: {
      main: "#161316",
      light: "#3A3939",
    },
    primary: {
      main: "#ff7691",
    },
  },
});

export default theme;
